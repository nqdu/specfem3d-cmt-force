----------------------------------------------------------------------
README
----------------------------------------------------------------------

This is a very simple example to explain the use of the CMT + FORCE simulation.

1. add a flag `USE_CMT_AND_FORCESOLUTION` in `Par_file`;
2. try `step1_run_simulation.sh`
3. plot figures by `step2_plot.py`
